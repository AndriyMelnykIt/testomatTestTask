{
  "name": "testomat-test-task",
  "version": "1.0.0",
  "private": true,
  "description": "Test task: simple REST server and client with sessions, retries, timeouts",
  "main": "BE/client/index.js",
  "scripts": {
    "start": "node BE/server.js",
    "test": "node ./node_modules/jest/bin/jest.js --runInBand",
    "dev": "cross-env DEBUG=server*,client* node BE/server.js",
    "fe:dev": "cd FE && npm run dev",
    "fe:build": "cd FE && npm run build",
    "fe:start": "cd FE && npm run start"
  },
  "dependencies": {
    "axios": "^1.7.7",
    "debug": "^4.3.7",
    "express": "^4.19.2",
    "cors": "^2.8.5",
    "cross-env": "^7.0.3"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}

